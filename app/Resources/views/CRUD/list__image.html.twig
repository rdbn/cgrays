{% extends 'SonataAdminBundle:CRUD:base_list_field.html.twig' %}

{% block field %}
    <div class="col-lg-3">
        <img src="{{ asset(object.image) }}" alt="{{ object.name }}" />
    </div>
{% endblock %}