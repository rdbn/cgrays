{% extends 'base.html.twig' %}

{% block title %}Вещи на продажу{% endblock %}

{% block body %}
    <div class="row">
    {% if is_granted("ROLE_USER") %}
        <div class="col-lg-12">
            <div class="btn-group">
                <button id="showListInventory" class="btn btn-warning" type="button" data-toggle="modal" data-target="#add-products-steam-user">
                    <span class="glyphicon glyphicon-plus"></span>
                    Добавить предмет для продажи
                </button>
                <button id="refresh-cache" class="btn btn-warning" type="button">
                    <span class="glyphicon glyphicon-refresh"></span>
                    Обновить кеш инвенторя
                </button>
                <button id="start-sell" class="btn btn-warning" type="button">
                    <span class="glyphicon glyphicon-play"></span>
                    Начать торговлю
                </button>
                <button id="stop-sell" class="btn btn-warning" type="button">
                    <span class="glyphicon glyphicon-stop"></span>
                    Остановить торговлю
                </button>
            </div>

            <div id="add-products-steam-user" class="modal fade" tabindex="-1" role="dialog" aria-labelledby="myLargeModalLabel">
                <div class="modal-dialog modal-lg" role="document">
                    <div class="modal-content">
                        <div class="modal-header">
                            <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                            <h4 class="modal-title">Содержимое инвентаря</h4>
                        </div>
                        <div class="modal-body">
                            <div id="user-inventory" class="row text-center"></div>
                            <nav id="menu-pagination">
                                <ul class="pager">
                                    <li>
                                        <button id="arrow-left" class="btn btn-warning"><span class="glyphicon glyphicon-arrow-left"></span></button>
                                    </li>
                                    <li>
                                        <button id="arrow-right" class="btn btn-warning"><span class="glyphicon glyphicon-arrow-right"></span></button>
                                    </li>
                                </ul>
                            </nav>
                        </div>
                        <div class="modal-footer">
                            <button id="close-modal" type="button" class="btn btn-danger" data-dismiss="modal">Закрыть</button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-lg-12">
            <h4 class="alert alert-warning">Выставленные предметы</h4>
            <div class="scroll-view">
                {% for product in products %}
                    <div class="col-xs-6 col-sm-4 col-md-2">
                        <div class="thumbnail">
                            <a class="view-sell-item" href="#" data-product="{{ product.product.id }}" data-product-price="{{ product.id }}" data-toggle="modal" data-target="#add-products-steam-user">
                                <img src="{{ asset(product.product.iconUrl) }}" alt="{{ product.product.name }}" />
                            </a>
                            <div class="caption">
                                <p>
                                    <strong><span class="text-warning price-item-sell">{{ product.price }} <span class="glyphicon glyphicon-rub"></span></span></strong>
                                </p>
                                <p class="text-center">
                                    <button class="btn btn-danger btn-sm remove-sell-item" type="button">Снять с продажи</button>
                                </p>
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </div>
        </div>
    {% else %}
        <div class="col-lg-12">
            <p>Для того, чтобы продавать предметы и получать за них реальные деньги, требуется выполнить вход {% include "SteamBundle:Login:login.html.twig" %}.</p>
        </div>
    {% endif %}
    </div>
{% endblock %}

{% block javascripts %}
    <script src="{{ asset('bundles/app/js/view-inventory.js') }}" type="text/javascript"></script>
{% endblock %}
